<%= form_with(model: user_trick) do |form| %>
  <% if user_trick.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_trick.errors.count, "error") %> prohibited this user_trick from being saved:</h2>

      <ul>
        <% user_trick.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :trick_id %>
    <%= form.text_field :trick_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
